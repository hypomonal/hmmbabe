# Copy this file to .env for local testing (never commit the real .env).
# In production, set this in your Vercel project's Environment Variables
# dashboard instead — it must never appear in client-side code.

# A GitHub Personal Access Token (classic) with "read:user" scope is enough.
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=
